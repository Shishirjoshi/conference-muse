# Conference Muse

A lightweight React + Vite template for conference and events dashboards.

Quick Start

1. Install dependencies

```bash
npm install
```

2. Run the dev server

```bash
npm run dev
```

3. Run tests

```bash
npm test
```

See `QUICK_START.md` and `VERIFICATION_CHECKLIST.md` for more details.
